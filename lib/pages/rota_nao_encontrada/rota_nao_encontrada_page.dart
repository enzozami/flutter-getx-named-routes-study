import 'package:flutter/material.dart';

class RotaNaoEncontradaPage extends StatelessWidget {
  const RotaNaoEncontradaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page não encontrada'),
      ),
      body: Container(),
    );
  }
}
